
* Change the Video Selector from a multi-Selector to a single-Selector - DONE
* Add The video title both on the left and the main panels - DONE
* Make the time stamp bigger, so that it is prominent
* Add a condition to filter out results if score is less than a threshold
* Add a frame on the right side where the youtube can be played
* Try RRF query, instead of just ELSER search